spring:
  flyway:
    locations: classpath:db/script/h2
  #    enabled: false  # To enable or disable the flyway
  #    baseline-on-migrate: true
  #    schemas: jbt_db
  h2:
    console:
      enabled: true
      path: /console
      settings:
        trace: true
        web-allow-others: false
  datasource:
    url: jdbc:h2:mem:jbt_db
    username: hello
    password: jbt
#    driver-class-name: org.h2.Driver

